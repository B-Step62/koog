# Module serialization-core

Core JSON serialization abstractions to support multiple serialization libraries.
Also provides support for kotlinx-serialization, because it has to be done at compile time.
